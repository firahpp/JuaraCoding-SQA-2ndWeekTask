# OS files
.vscode/               # Folder konfigurasi VSCode (jika kamu menggunakan VSCode)
.DS_Store              # File metadata macOS
Thumbs.db              # File metadata Windows

# Maven-specific folders/files
target/                # Folder hasil build Maven (seperti .class files, JARs, dll.)
pom.xml.tag
pom.xml.bak
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.txt

# Log files
*.log

# Generated reports/screenshots
test-output/           # Folder default untuk report TestNG
extent-reports/        # (Opsional) Jika kamu menggunakan ExtentReports
screenshots/           # (Opsional) Jika kamu menyimpan screenshot error

# Security/Credentials (Penting! Jika kamu punya file konfigurasi sensitif)
# Contoh: Jika ada file di folder resources yang berisi API keys atau password
# application.properties
# config.properties